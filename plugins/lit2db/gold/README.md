# gold/

Hand-annotated gold sets, one JSONL per project. Sizing + inter-annotator-agreement requirements: blueprint section 8 gate 1. An unreliable gold set silently corrupts every threshold calibrated against it.
